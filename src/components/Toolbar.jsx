const Toolbar = (props) => {
  return (
    <nav id="toolbar">
      <button>Text</button>
      <button>Table</button>
      <button>Image</button>
      <button>Canvas</button>
    </nav>
  )
}

export default Toolbar;